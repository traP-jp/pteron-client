import ErrorBoundary from "/@/components/ErrorBoundary";

const Checkout = () => {
    return (
        <ErrorBoundary>
            <h1>Checkout</h1>
        </ErrorBoundary>
    );
};

export default Checkout;
